---
import Container from '@/components/Container.astro';
import Link from '@/components/ui/Link.astro';
import RootLayout from '@/layouts/RootLayout.astro';

let title = '404 Resource Not Found';
let description = 'My apologize, but the resource you were looking for is not found';
---

<RootLayout seo={{ title, description }}>
  <section class='grid h-[calc(100vh-4rem)] place-items-center'>
    <Container className='text-center'>
      <h1 class='text-title pb-4'>{title}</h1>
      <p class='pb-3'>{description}</p>
      <Link
        href='/'
        class='items-center rounded-lg bg-primary px-3 py-1.5 text-sm text-primary-foreground transition hover:bg-primary/50'
        >&crarr; Back</Link
      >
    </Container>
  </section>
</RootLayout>
